export default function Encabezado({titulo}) {
  return (
    <header style={{padding: '1rem 0'}}>
      <h1>{titulo}</h1>
      <p>Bienvenido a tu primer componente profesional</p>
    </header>
  )
}